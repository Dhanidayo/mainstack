// import * as chaiHttp from "chai-http";
// import { Request, SuperAgentRequest, Response } from "superagent";

// export interface CustomAgent extends ChaiHttp.Agent {
//   (app: any): ChaiHttp.Agent;
//   agent: ChaiHttp.Agent;
//   close(callback?: (err?: any) => void): void;
//   // Add other necessary properties from Agent
//   // ...
//   // Add the string index signature
//   [key: string]: (path: string) => SuperAgentRequest & Promise<Response>;
// }
